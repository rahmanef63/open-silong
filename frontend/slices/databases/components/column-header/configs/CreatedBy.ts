import type { PropertyTypeMenuConfig } from "../types";

/** Created by — no edit_property (nothing to configure), no change_type,
 *  no ai_autofill, no freeze (system field). */
export const CreatedByConfig: PropertyTypeMenuConfig = {
  mainMenu: [
    "filter", "sort", "group", "calculate",
    "hide", "wrap_content",
    "insert_left", "insert_right",
    "duplicate", "delete",
  ],
};
