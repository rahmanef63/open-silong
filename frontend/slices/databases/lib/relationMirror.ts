/** @deprecated — moved to `@/shared/lib/databases/relationMirror` so the
 *  shared store can consume it without inverting the shared → slice
 *  boundary. Thin re-export for back-compat. */
export * from "@/shared/lib/databases/relationMirror";
