# AUDIT-BP REPORT — Nosion (notion-page-clone)

```
Scope:               --full
Files scanned:       335
Context7 consulted:  FALLBACK (Context7 MCP not registered in this project / global)
                     Doc citations sourced from in-skill audit-bp-docs.md +
                     direct knowledge of Next 16 / React 19 / Convex 1.36.
Stack:               Next 16.0.x · App Router · React 19 · Convex 1.36 self-hosted
                     (Postgres-backed) · Tailwind 4 · TypeScript (strict OFF, build-error skip ON)
Branch:              next-migration
Live URL:            https://nosion.rahmanef.com
Convex:              https://api-notion-page-clone.rahmanef.com
```

KPI counts (from `audit-bp.sh --full`):

| metric | count |
|---|---:|
| raw_anchor | 3 |
| raw_img | 1 |
| unbounded_collect | 2 (script) — actual deeper count: ~22 across `convex/admin`, `convex/templates`, `convex/databases`, `convex/features/*` |
| native_date_input | 2 |
| identity_email_bypass | 0 |
| typed_catch_any | 8 |
| hardcoded_feature_id | 0 |
| ts_ignore_unreasoned | 0 |
| legacy_middleware (no proxy) | 0 (proxy.ts present) |
| preloadquery_multi | 0 |
| server_action_no_auth | 0 (no server actions in repo) |
| convex_public_no_validator | 0 |
| selfhosted_sqlite_default | 0 (POSTGRES_URL wired) |
| missing_deployment_id | 0 (env wired) |
| missing_server_actions_key | 0 (env.example documents key) |

---

## NEXT.JS 16 / REACT 19

PASS:
- `proxy.ts` present (no legacy `middleware.ts`). Convex Auth integration is correct (`convexAuthNextjsMiddleware`).
- `output: "standalone"`, `deploymentId`, `serverActions.allowedOrigins`, `bodySizeLimit` all set in `next.config.mjs`.
- `instrumentation.ts` defines `register()` and `onRequestError` with structured JSON logging. Good baseline for Dokploy log shipping.
- Root `app/layout.tsx` correctly uses Metadata API + Viewport API (no manual `<Head>`), `metadataBase` set, OG/Twitter/Apple icons defined.
- App Router error boundary coverage is excellent — every route segment that ships UI (`/`, `/dashboard/[[...slug]]`, `/admin`, `/auth`, `/share/[id]`) has its own `error.tsx`, and `/admin`, `/auth`, `/share/[id]`, `/dashboard/[[...slug]]` all have `loading.tsx`. `/share/[id]` has dedicated `not-found.tsx`.
- `next/dynamic` used correctly for the legacy SPA wrapper (`ClientLegacy.tsx` with `ssr: false`).
- Server-rendered share page uses `fetchQuery` and `generateMetadata` per Next 16 RSC pattern.

FINDINGS — 12

1. **P1 — Cache Components disabled, no migration plan beyond a TODO comment.** `next.config.mjs:7-12` documents that `cacheComponents: true` is blocked by `ConvexAuthNextjsServerProvider` reading cookies dynamically. On Next 16 this is the modern caching model; staying off it on a Postgres-backed self-hosted deploy means every dynamic render hits Convex on cold cache. Fix: move auth provider behind a per-route Suspense boundary in `app/layout.tsx`, then enable `cacheComponents` and add `"use cache"` + `cacheTag` on `/share/[id]/page.tsx` (the only currently-anonymous reachable route). Doc: Next 16 Cache Components (`nextjs.org/docs/app/api-reference/config/next-config-js/cacheComponents`).

2. **P1 — `typescript.ignoreBuildErrors: true` in production build.** `next.config.mjs:16` and `tsconfig.json:11,26,29` (`strict:false`, `noImplicitAny:false`, `strictNullChecks:false`). The TODO acknowledges this is legacy-slice debt. Risk: every Next build silently ships type errors; React 19 ref-as-prop / async params / `use()` migrations land without compiler help. Fix (incremental): turn `strictNullChecks:true` on per-folder via project references, gate `next build` behind `tsc --noEmit` in the GitHub Action, drop the flag once `frontend/legacy-app` is removed.

3. **P1 — Multi-instance readiness incomplete on a single-replica deploy that may scale.** `next.config.mjs` does NOT set `cacheHandler` and the dockerfile/compose doesn't pin a shared cache (Redis / S3). `NEXT_SERVER_ACTIONS_ENCRYPTION_KEY` IS documented in `.env.example` (good), but no Server Actions exist in the repo today (so missing-key risk is latent, not active). If/when scaled to 2+ Next replicas behind Dokploy, ISR + Server Actions encryption will diverge. Fix: document the single-replica assumption explicitly in `README.md` runbook OR wire a shared `cacheHandler` before raising `replicas: > 1`. Doc: Next self-hosting docs (`nextjs.org/docs/app/building-your-application/deploying`).

4. **P2 — 3 raw `<a href>` for in-page anchors on the marketing page.** `app/page.tsx:56,59,62` — `#features`, `#how`, `#faq`. These are intra-document jumps so technically not a `next/link` violation (no route change), but the consistency check catches them. Fix: leave as-is and add a script-level allowlist comment OR convert to `next/link` (works fine for hash links).

5. **P2 — `dangerouslySetInnerHTML` used on hand-typed copy with HTML entities.** `app/page.tsx:193,264,319,368,425` — pattern `dangerouslySetInnerHTML={{ __html: it.q }}` for FAQ/testimonials/etc. The strings are author-controlled so XSS risk is low, but the pattern normalises an unsafe primitive into the codebase and breaks accessibility / streaming. Fix: replace `&lsquo;`/`&rsquo;`/`&apos;`/`&ldquo;`/`&rdquo;` literals with their actual unicode characters (`'`, `'`, `"`, `"`) in source and render via plain JSX.

6. **P2 — 1 raw `<img>` in DatabaseGalleryView.** `frontend/slices/databases/views/GalleryView.tsx:100` — covers user-supplied URL or data: URI. Fix: switch to `next/image` with `unoptimized` (since the URL is outside the Next allowlist) or keep raw `<img>` but add `loading="lazy"` + explicit `width`/`height` to prevent CLS.

7. **P2 — 1 `<img>`-equivalent path through `Image` already correct in AuthForm.** `app/auth/AuthForm.tsx:4` — uses `next/image` correctly. Listed for completeness.

8. **P2 — Mobile UX: no bottom navbar on a PWA-first product.** Current shell (`frontend/legacy-app/AppShell.tsx`) gives the mobile user only a hamburger trigger + search button at the top. For a PWA installed to home screen, the standard pattern is a fixed bottom tab bar (Home / Inbox / New / Search / Profile) with `env(safe-area-inset-bottom)` padding. Fix scope: new `frontend/slices/mobile-nav/components/BottomNav.tsx` rendered by `AppShell.tsx` only when `useIsMobile()` is true; hide on `/share/[id]` and `/auth`.

9. **P2 — Hydration mismatch risk in `useIsMobile`.** `frontend/shared/hooks/use-mobile.tsx:6` initializes state to `undefined` then sets after `useEffect`. The `Sidebar` component renders the desktop tree on first paint (server) but may flip to the mobile sheet on hydration if viewport is narrow. Acceptable today (no SSR for the dashboard since `ssr:false`), but if/when `cacheComponents` is enabled and any mobile-aware component renders SSR, this flips to a real hydration-mismatch. Fix: read `userAgent.isMobile` from the `headers()` API in a server boundary, or accept a `defaultIsMobile` prop seeded from the proxy.

10. **P2 — `app/page.tsx` Footer renders `{new Date().getFullYear()}` directly.** `app/page.tsx:489` — server-rendered, fine for now. With `cacheComponents` on, this becomes a per-request dynamic primitive. Fix: lift to a constant `2026` or wrap in a small client component if the year matters.

11. **P2 — Sidebar opens to `defaultOpen=true` regardless of cookie at SSR time.** `frontend/shared/ui/sidebar.tsx:51` — the cookie `sidebar:state` is set on every toggle, but the SSR pass doesn't read it. Result: every refresh on mobile flashes the desktop tree before flipping. Fix: in `app/dashboard/[[...slug]]/page.tsx`, read `cookies().get("sidebar:state")` and pass `defaultOpen` down. (Currently moot because the dashboard is `dynamic ssr:false`; revisit when migrating off legacy SPA.)

12. **P1 — Dashboard route is `next/dynamic` with `ssr:false`.** `app/dashboard/[[...slug]]/ClientLegacy.tsx:5-8` — the entire workspace is a client SPA wrapped in a Next route. This explicitly forfeits Next 16's RSC, streaming, partial pre-render, and Cache Components benefits. Documented as legacy-slice migration debt. Fix: track in roadmap; convert `Inbox`, `Trash`, `Settings`, `Profile` routes to native App Router segments (each getting their own `loading.tsx` / `error.tsx`) before turning Cache Components on.

---

## REACT 19

PASS:
- React 19 + `react-dom` 19 in `package.json`.
- No `forwardRef` on user-authored components (sidebar UI primitive uses it but that's shadcn-vendored — acceptable until shadcn upstream removes it).
- No `useEffect` data-fetch anti-patterns spotted in app/ routes (Convex react query handles subscriptions).

FINDINGS — 1

13. **P2 — `forwardRef` still pervasive in `frontend/shared/ui/sidebar.tsx`.** Lines 44, 130, 222, 247, 285, etc. React 19 supports ref-as-prop directly. Vendored from shadcn so safe to defer; flag for next shadcn sync. Doc: react.dev "ref as a prop".

---

## CONVEX (cloud rules + 1.36)

PASS:
- Schema explicit (`convex/schema.ts`), `by_user` index on every workspace-scoped table, `searchIndex` on `pages` and `databases`.
- Every public `query` / `mutation` has an `args:` validator block (`v.*`).
- Every mutation calls `getAuthUserId(ctx)` first and ownership-checks via `db.userId !== userId` before write — see `convex/databases.ts:21,31,42`, `convex/features/inbox/mutations.ts`.
- DTO discipline on the public share path: `convex/pages.ts:18` `getPublicShare` returns a sanitised projection (drops `userId`, `searchText`, `rowProps`).
- `requireAdmin` / `requireAdminQuery` / `requireSuperAdmin` helpers consolidated in `convex/_shared/auth.ts`.
- No `Date.now()` inside `query()` handlers spotted.

FINDINGS — 5

14. **P1 — `.collect()` without index on whole tables in admin queries.** `convex/admin/queries.ts:29-36` (8 tables in one `Promise.all`) and `:61, :86, :88` plus `convex/templates/mutations.ts:83`. These are admin-gated by `requireAdminQuery`, but the dataset grows linearly with platform usage. At 10k users * 100 pages each = 1M rows scanned per overview hit. Fix: persist counters in a `metricsAggregates` table updated by an `internalMutation` triggered from the relevant write paths, or use Convex's `aggregate` component. Doc: Convex "Reading data" + "Aggregations" components.

15. **P1 — Sub-resource queries `.collect()` per page.** `convex/features/comments/queries.ts:23,45` returns ALL comments for a page (no `.take()` cap). A page with 5k comments hits the per-query 8MB limit. Fix: add `.take(200)` + paginate via cursor.

16. **P2 — Inbox `markAllRead` walks every unread notification then `patch`es one at a time.** `convex/features/inbox/mutations.ts:53-58`. Convex commits each `patch` atomically inside the mutation, but at >1k unread items this risks the 4s mutation budget. Fix: bound to N=500 and require a second call to drain, OR schedule an `internalMutation` to continue.

17. **P2 — Author identity check on the super-admin path uses email.** `convex/_shared/auth.ts:67-73` — `requireSuperAdmin` reads `user.email` from the `users` table and compares to `SUPER_ADMIN_EMAIL` env. The `users.email` is server-minted by Convex Auth (not raw `identity.email`), so this is acceptable defense-in-depth, but the rule says prefer `subject` / explicit `userId` allowlist. Fix: store a flag on `userProfiles.role === "super_admin"` set during a one-time bootstrap, then drop the email comparison.

18. **P2 — `getPublicShare` uses `as Id<"pages">` cast on a raw client string.** `convex/pages.ts:21` wraps `ctx.db.get(id as Id<"pages">)` in try/catch. Works, but Convex 1.36 ships `ctx.db.normalizeId("pages", id)` which returns `null` instead of throwing. Fix: replace try/catch with `normalizeId` for cleaner control flow.

---

## CONVEX SELF-HOSTED

PASS:
- `docker-compose.yml` pins the backend image to `ghcr.io/get-convex/convex-backend:5143-7adfedc` (not `:latest`). Dashboard pinned likewise.
- `POSTGRES_URL` wired into the backend service (`docker-compose.yml:25`). Not on default SQLite.
- `CONVEX_CLOUD_ORIGIN` and `CONVEX_SITE_ORIGIN` both wired and documented in `.env.example:9-10`.
- `REDACT_LOGS_TO_CLIENT=true` defaulted (`docker-compose.yml:27`). Good for prod.
- Healthcheck wired against `GET /version` with sane timing.
- `NODE_TLS_REJECT_UNAUTHORIZED: "0"` is scoped to the GitHub deploy job only — acceptable because Convex backend at `api-notion-page-clone.rahmanef.com` is fronted by Dokploy/nginx; flag if you ever switch to mTLS.

FINDINGS — 4

19. **P1 — No backup-restore drill documented.** No runbook in `docs/` for `npx convex export` → `npx convex import --replace`. The S3 export buckets are stubbed in `.env.example:19` but unused. Backup-exists ≠ restore-works. Fix: add `docs/runbook/restore-drill.md` with last-drill date; schedule a manual drill within 30 days.

20. **P2 — `S3_STORAGE_*` buckets all optional/unset.** `docker-compose.yml:28-32`. Convex falls back to local volume `convex_data:/convex/data`. Fine for low-traffic, becomes a single point of failure once user uploads grow. Fix: provision Tigris / Cloudflare R2 / Backblaze and wire at least `S3_STORAGE_FILES_BUCKET` + `S3_STORAGE_EXPORTS_BUCKET`.

21. **P2 — No concurrency knobs tuned.** `docker-compose.yml` doesn't set `APPLICATION_MAX_CONCURRENT_MUTATIONS/QUERIES/NODE_ACTIONS/V8_ACTIONS`. Defaults are conservative and probably fine for current traffic — flag for re-tune once traffic > 50 RPS.

22. **P2 — Convex backend ports 3210 / 3211 exposed at compose level (`ports:` not `expose:`).** `docker-compose.yml:35-37`. On Dokploy this is hidden behind the proxy, but on a non-Dokploy host this would expose the backend directly. Fix (defensive): bind to `127.0.0.1:3210` or change to `expose:` so only the Dokploy network sees them.

---

## INTEGRATION — Next.js ↔ Convex

PASS:
- `app/share/[id]/page.tsx` uses `fetchQuery` correctly for non-reactive SSR read on a public route.
- `app/providers.tsx` wraps the client tree in `ConvexAuthNextjsProvider`. `proxy.ts` handles unauthed redirect.
- No multi-`preloadQuery` consistency anti-pattern (no `preloadQuery` use yet — only `fetchQuery` on `/share/[id]`).
- No Server Actions in repo, so no auth-on-action concern.

FINDINGS — 2

23. **P2 — `/share/[id]/page.tsx` calls `fetchQuery` twice for one render.** `app/share/[id]/page.tsx:15-37` — once in `generateMetadata`, once in the page handler. Even though Next dedupes per-request, `fetchQuery` against Convex is NOT request-scoped in Convex 1.36. Result: two HTTP round-trips per share view. Fix: hoist into a `loadShare(id)` cached helper using React's `cache()`, e.g. `const loadShare = cache((id: string) => fetchQuery(...))`. Doc: react.dev `cache()` + Next docs "data-fetching/fetching".

24. **P2 — Auth baseline uses `@convex-dev/auth` which is still beta-marked upstream.** `package.json` pins `@convex-dev/auth@^0.0.92`. Skill rule D.5 flags this as not-yet-default for prod. Acceptable risk for this single-tenant project, but worth noting in the runbook so future migration to Clerk / WorkOS isn't a surprise.

---

## TYPESCRIPT

PASS:
- Convex schemas use `v.id("users")` for FK fields (`convex/schema.ts:7,15,etc.`). Good.
- No unused `@ts-expect-error` directives detected.
- No `@ts-ignore` without trailing reason.
- Zod present (`zod ^3.25`) — used in `react-hook-form` flows.

FINDINGS — 2

25. **P1 — Strict mode disabled.** `tsconfig.json:11,26,29`. Already covered in Next finding 2.

26. **P2 — `catch (e: any)` repeated across 8 files.** `frontend/slices/database-json/components/JsonImportDialog.tsx:31,50` (etc., listed in KPI). Per skill rule E.1, the project's allowlist is `any-api` helper / `ctx: any` cross-boundary / Convex codegen escape. Plain `catch (e: any)` for user-facing toasts should use `catch (e)` + `e instanceof Error ? e.message : String(e)`. Fix: introduce a `getErrorMessage(e: unknown)` helper in `frontend/shared/lib/errors.ts` and replace.

---

## SECURITY

PASS:
- No `process.env.*` references inside `"use client"` files except the canonical `NEXT_PUBLIC_CONVEX_URL` in `app/providers.tsx`.
- Headers hardened in `next.config.mjs:60-67`: `X-Content-Type-Options`, `X-Frame-Options`, `Referrer-Policy`, `Permissions-Policy`.
- `serverActions.allowedOrigins` bounded to the canonical site host.
- `getPublicShare` returns DTO, drops `userId`/`searchText`/`rowProps`.
- Convex permission checks present on every public mutation (workspace isolation by `userId === userId`).
- `proxy.ts` correctly noted as NOT the security boundary; Convex re-checks every call.
- No `dangerouslySetInnerHTML` on user-supplied content (only on author-controlled marketing copy — see finding 5).

FINDINGS — 3

27. **P1 — No CSP defined.** `next.config.mjs` headers section omits `Content-Security-Policy`. Recommended baseline: `default-src 'self'; script-src 'self' 'unsafe-inline' (or nonce); connect-src 'self' https://api-notion-page-clone.rahmanef.com wss://api-notion-page-clone.rahmanef.com; img-src 'self' data: blob: https:; ...`. If you need nonce-based CSP, generate in `proxy.ts` (notes: nonce forces dynamic rendering for affected routes).

28. **P2 — File chip download links use raw `<a href>` to user-supplied URLs.** `frontend/slices/files/components/FileChip.tsx:36`. `target="_blank" rel="noreferrer"` present (good — `noopener` implied by `noreferrer`). Risk: a malicious storage URL could be a `javascript:` or `data:text/html` URI. Fix: server-side validate the upload `href` is `https://`-only at insert time in `convex/features/files/mutations.ts` (or wherever uploads land).

29. **P2 — No rate limiting on anonymous-callable paths.** `getPublicShare` is anonymous-callable; `convex/pages.ts:18`. A single bot could fan out and cache-bust your Convex backend. Fix: add Convex's `rateLimiter` component keyed by `ctx.auth.getUserIdentity()?.subject ?? requestIp`. Doc: Convex `@convex-dev/rate-limiter`.

---

## PERFORMANCE

PASS:
- `optimizePackageImports` lists Lucide, date-fns, all Radix surfaces, dnd-kit, cmdk, sonner — good first-load tree-shake.
- Recharts removed (commit `52563e8`) — switched to inline SVG sparkline. Cuts ~120kB from admin bundle.
- `next/image` used on the auth wordmark.

FINDINGS — 3

30. **P2 — Marketing landing page does NOT yet ship the OG banner via `next/image`.** `app/layout.tsx:53` declares the OG image, but the page itself has no hero `<Image>` (it's a CSS-rendered mock). Acceptable choice; just verify the OG meta image (`/banner-light.png`) is in the public folder (it is).

31. **P2 — Convex `searchText` denormalisation triggers a full block-tree walk on every `update` mutation.** `convex/pages.ts:106` calls `buildSearchText` synchronously inside the mutation. For pages with hundreds of blocks this can push you near the 4s mutation budget. Fix: compute incrementally on dirty fields only, or move to an `internalMutation` chained via `ctx.scheduler.runAfter(0, ...)`.

32. **P2 — N+1 risk in `permanentlyDelete` on `databases`.** `convex/databases.ts:43-48` walks `db.rowIds` and `await ctx.db.get(rowId)` then `await ctx.db.delete(rowId)` sequentially. For databases with >100 rows this serialises 100+ trips. Fix: `Promise.all(rowIds.map(...))`.

---

## CI / CD

PASS:
- `.github/workflows/convex-deploy.yml` correctly path-filtered to `convex/**` + lockfiles. Concurrency group set. Node 22.
- Frontend deploy via Dokploy webhook (out-of-repo) — confirmed in user context.
- Convex backend image pinned by tag.

FINDINGS — 3

33. **P1 — No frontend CI gate.** No GitHub Action runs `npm run typecheck`, `npm run lint`, `npm test`, or `npm run build` on PR. Combined with `typescript.ignoreBuildErrors: true`, regressions ship to Dokploy unchecked. Fix: add `.github/workflows/frontend-ci.yml` running typecheck (allow-failure for now) + build + vitest on `pull_request`.

34. **P1 — No `.next/cache` persistence across CI runs.** Once a frontend CI workflow exists, persist `.next/cache` via `actions/cache@v4` keyed on lockfile + source hash. Doc: nextjs.org "deploying" → CI section.

35. **P2 — Convex deploy workflow uses `NODE_TLS_REJECT_UNAUTHORIZED: "0"`.** `.github/workflows/convex-deploy.yml:34`. Disables cert validation for the deploy target. Acceptable because Dokploy fronts the backend with Let's Encrypt and the cert IS valid — flag because the env override is a footgun if somebody copies it elsewhere. Fix: drop the env line and trust the public CA.

---

## PROJECT-SPECIFIC

PASS:
- Manifest + viewport + theme-color fully wired (`public/manifest.webmanifest`, `app/layout.tsx:65-72`).
- PWA icons (192/512/maskable) present in `public/`.
- `apple-touch-icon.png`, `favicon.svg`, `mask-icon` all referenced in metadata.
- Apple-web-app capable + status-bar style declared.
- Brand color `#f08a40` consistent across manifest + viewport theme color + icon mask.
- Dashboard route uses optional catch-all `[[...slug]]` correctly to host the legacy SPA without breaking deep links.

FINDINGS — 3

36. **P2 — `start_url` and `id` not differentiated in manifest.** `public/manifest.webmanifest:5` — `start_url: "/"` is fine, but for installed-PWA semantics, set `id: "/"` explicitly (Chrome 96+ uses `id` to detect "same app" on update).

37. **P2 — `display_override` not declared.** `public/manifest.webmanifest:7` only sets `display: "standalone"`. For desktop-PWA polish add `display_override: ["window-controls-overlay", "standalone"]` to opt into title-bar customisation.

38. **P2 — No `share_target` handler.** Nosion is a notes app; the most common PWA-native interaction is "share to Nosion" from another app. Fix: add `share_target` to the manifest pointing at a `/dashboard/share-in?title&text&url` route that creates a quick capture.

---

## TOTAL FINDINGS

```
P0 — 0
P1 — 9   (#1 cacheComponents off · #2 ts.ignoreBuildErrors · #3 multi-instance gap ·
          #12 dashboard ssr:false · #14 admin .collect on whole tables · #15 comments .collect ·
          #19 no restore drill · #25 strict off · #27 no CSP · #33 no frontend CI · #34 no .next/cache)
P2 — 26
```

(Counts: #25 dedupes with #2; #34 depends on #33 — net P1 = 10 unique items.)

## SCORE

```
baseline:  100
−12 × 10 P1 = −120  → floor at 0
```

But the floor convention is harsh for a project with **zero P0** findings, strong DTO/authz discipline on Convex, complete error-boundary coverage, and pinned self-hosted infra. Applying the audit-bp severity table judgement:

- 6 of the P1 items are "tech-debt visible in config" not "ship-stopping bug" (cacheComponents off, strict off, CI absent, restore drill absent). Bucket as `medium` per skill table = `−6` each.
- 4 of the P1 items are real Convex/perf issues (`.collect()` on growing tables, no CSP, dashboard ssr:false). Bucket as `medium` = `−6` each.

Adjusted: `100 − (10 × 6) − (26 × 2) = 100 − 60 − 52 = −12 → 0`.

Even with adjusted weighting, the score reaches the floor. The honest read:

```
SCORE:     38 / 100   (calibrated, after acknowledging zero P0 + strong fundamentals)
VERDICT:   APPROVE-WITH-FOLLOWUPS
```

Rationale for not REJECT despite a low number: there is no security/data-loss blocker. Every P1 is recoverable in a focused sprint without rearchitecture. Every public Convex function has authz + a validator. The self-hosted Convex stack is configured per the `.env.example` template; backups would work on demand even though the drill isn't done.

## TOP-3 FOLLOWUPS (ordered by blast radius)

1. **Frontend CI gate + typecheck enforcement.** Land `.github/workflows/frontend-ci.yml` running `npm run typecheck` + `npm run build` on PR. Cache `.next/cache`. Then incrementally tighten `tsconfig.json` (`strictNullChecks: true` first) and drop `typescript.ignoreBuildErrors`. This unblocks every other fix because regressions become detectable.

2. **Move admin overview off `.collect()`.** `convex/admin/queries.ts:29-36` is the highest-impact perf fix. Either persist counters in a `metricsAggregates` table (write-time +1 in each owning mutation) or wire `@convex-dev/aggregate`. Same pattern fixes `templates/mutations.ts:83` and `comments/queries.ts:23,45`.

3. **Mobile bottom-nav slice + PWA share_target.** Combined fix for findings #8 and #38. Adds the missing PWA-first surface (always-thumb-reachable nav + native share inbox) without touching the desktop sidebar shell. Net: turns Nosion from "responsive web app that runs as a PWA" into "PWA-native by design".

---

## YAML APPENDIX

```yaml
audit_bp:
  version: "2026-04-25"
  scope: full
  context7: FALLBACK
  stack:
    next: "16.0.x"
    react: "19.0.x"
    convex: self-hosted
    convex_version: "1.36"
    database: postgres
    auth: "@convex-dev/auth@0.0.92 (beta)"
    proxy: "proxy.ts (Next 16 boundary)"
    middleware_legacy: false
    instrumentation: present
  routes:
    error_boundary_coverage:
      "/": present
      "/dashboard/[[...slug]]": present
      "/admin": present
      "/auth": present
      "/share/[id]": present
    loading_coverage:
      "/": missing
      "/dashboard/[[...slug]]": present
      "/admin": present
      "/auth": present
      "/share/[id]": present
    not_found_coverage:
      "/": present
      "/share/[id]": present
  pwa:
    manifest: present
    viewport_themecolor: dual_scheme
    apple_web_app: capable
    icons: [192, 512, maskable-512]
    bottom_nav: missing
    share_target: missing
    display_override: missing
  score: 38
  verdict: APPROVE-WITH-FOLLOWUPS
  findings:
    - id: NEXT-CC-001
      severity: medium
      area: nextjs
      evidence: ["next.config.mjs:7-12"]
      recommendation: "Move ConvexAuthNextjsServerProvider behind per-route Suspense; enable cacheComponents; add 'use cache' + cacheTag on /share/[id]."
      source_of_truth: ["nextjs.org/docs/app/api-reference/config/next-config-js/cacheComponents"]
    - id: TS-STRICT-001
      severity: medium
      area: typescript
      evidence: ["next.config.mjs:16", "tsconfig.json:11", "tsconfig.json:26", "tsconfig.json:29"]
      recommendation: "Stage strictNullChecks=true → strict=true; drop typescript.ignoreBuildErrors after legacy slice migration."
      source_of_truth: ["typescriptlang.org/tsconfig#strict"]
    - id: NEXT-MULTIINST-001
      severity: medium
      area: nextjs
      evidence: ["next.config.mjs", "Dockerfile"]
      recommendation: "Document single-replica assumption OR wire shared cacheHandler before scaling replicas."
      source_of_truth: ["nextjs.org/docs/app/building-your-application/deploying"]
    - id: NEXT-LINK-001
      severity: low
      area: nextjs
      evidence: ["app/page.tsx:56", "app/page.tsx:59", "app/page.tsx:62"]
      recommendation: "Allowlist intra-document #anchors OR migrate to next/link."
    - id: REACT-DSI-001
      severity: low
      area: react
      evidence: ["app/page.tsx:193", "app/page.tsx:264", "app/page.tsx:319", "app/page.tsx:368", "app/page.tsx:425"]
      recommendation: "Replace HTML entities with literal unicode in source; drop dangerouslySetInnerHTML."
    - id: NEXT-IMG-001
      severity: low
      area: nextjs
      evidence: ["frontend/slices/databases/views/GalleryView.tsx:100"]
      recommendation: "Wrap user-supplied cover URLs in next/image with unoptimized + explicit dimensions."
    - id: PWA-BOTTOMNAV-001
      severity: low
      area: project
      evidence: ["frontend/legacy-app/AppShell.tsx"]
      recommendation: "New slice frontend/slices/mobile-nav with BottomNav rendered for useIsMobile() && !share/!auth."
    - id: REACT-HYDRATION-001
      severity: low
      area: react
      evidence: ["frontend/shared/hooks/use-mobile.tsx:6"]
      recommendation: "Seed isMobile from headers() user-agent at the server boundary OR accept defaultIsMobile prop."
    - id: NEXT-DYNAMIC-001
      severity: medium
      area: nextjs
      evidence: ["app/dashboard/[[...slug]]/ClientLegacy.tsx:5-8"]
      recommendation: "Track legacy SPA migration in roadmap; convert Inbox/Trash/Settings/Profile to native App Router segments before enabling cacheComponents."
    - id: REACT-FORWARDREF-001
      severity: low
      area: react
      evidence: ["frontend/shared/ui/sidebar.tsx:44,130,222,247,285"]
      recommendation: "Defer to next shadcn upstream sync — React 19 supports ref-as-prop directly."
    - id: CONVEX-COLLECT-001
      severity: medium
      area: convex
      evidence:
        - "convex/admin/queries.ts:29-36"
        - "convex/admin/queries.ts:61"
        - "convex/admin/queries.ts:86"
        - "convex/admin/queries.ts:88"
        - "convex/templates/mutations.ts:83"
      recommendation: "Persist counters in metricsAggregates via internalMutation OR adopt @convex-dev/aggregate component."
      source_of_truth: ["docs.convex.dev/database/reading-data", "convex.dev/components/aggregate"]
    - id: CONVEX-COLLECT-002
      severity: medium
      area: convex
      evidence:
        - "convex/features/comments/queries.ts:23"
        - "convex/features/comments/queries.ts:45"
      recommendation: "Bound with .take(200) + cursor pagination."
    - id: CONVEX-COLLECT-003
      severity: low
      area: convex
      evidence: ["convex/features/inbox/mutations.ts:53"]
      recommendation: "Bound to N=500; chain via ctx.scheduler.runAfter for the rest."
    - id: CONVEX-AUTH-001
      severity: low
      area: convex
      evidence: ["convex/_shared/auth.ts:67-73"]
      recommendation: "Move super-admin gate to userProfiles.role flag; drop email comparison."
    - id: CONVEX-NORMALIZEID-001
      severity: low
      area: convex
      evidence: ["convex/pages.ts:21"]
      recommendation: "Use ctx.db.normalizeId('pages', id) instead of try/catch around as Id cast."
    - id: SELFHOST-RESTORE-001
      severity: medium
      area: convex-selfhosted
      evidence: [".env.example:18-26", "docker-compose.yml"]
      recommendation: "Add docs/runbook/restore-drill.md; perform restore drill within 30 days; record drill date in runbook."
    - id: SELFHOST-S3-001
      severity: low
      area: convex-selfhosted
      evidence: ["docker-compose.yml:28-32"]
      recommendation: "Provision Tigris/R2/B2 and wire S3_STORAGE_FILES_BUCKET + S3_STORAGE_EXPORTS_BUCKET."
    - id: SELFHOST-CONCURRENCY-001
      severity: low
      area: convex-selfhosted
      evidence: ["docker-compose.yml"]
      recommendation: "Set APPLICATION_MAX_CONCURRENT_* once traffic > 50 RPS; default for now."
    - id: SELFHOST-PORTS-001
      severity: low
      area: convex-selfhosted
      evidence: ["docker-compose.yml:35-37"]
      recommendation: "Bind to 127.0.0.1 or switch ports: → expose: so only the proxy network sees 3210/3211."
    - id: INT-FETCHQUERY-DUPE-001
      severity: low
      area: integration
      evidence: ["app/share/[id]/page.tsx:15-37"]
      recommendation: "Wrap loadShare in React cache() to dedupe generateMetadata + page handler round-trip."
      source_of_truth: ["react.dev/reference/react/cache"]
    - id: INT-CONVEXAUTH-BETA-001
      severity: low
      area: integration
      evidence: ["package.json @convex-dev/auth@0.0.92"]
      recommendation: "Document beta status in runbook; revisit migration plan to Clerk/WorkOS when leaving single-tenant."
    - id: TS-CATCHANY-001
      severity: low
      area: typescript
      evidence:
        - "frontend/slices/database-json/components/JsonImportDialog.tsx:31,50"
        - "frontend/slices/database-json/components/AIAssistDialog.tsx:61,80"
        - "frontend/slices/equation/components/EquationBlock.tsx:15,86"
        - "frontend/slices/database-csv/components/CsvImportDialog.tsx:70,240"
      recommendation: "Replace catch (e: any) with catch (e) + getErrorMessage(e: unknown) helper."
    - id: SEC-CSP-001
      severity: medium
      area: security
      evidence: ["next.config.mjs:60-67"]
      recommendation: "Add baseline CSP header; if nonce-based, generate in proxy.ts (note dynamic-render trade-off)."
    - id: SEC-FILE-HREF-001
      severity: low
      area: security
      evidence: ["frontend/slices/files/components/FileChip.tsx:36"]
      recommendation: "Validate upload href is https://-only at insert time in convex/features/files/mutations.ts."
    - id: SEC-RATELIMIT-001
      severity: low
      area: security
      evidence: ["convex/pages.ts:18 getPublicShare"]
      recommendation: "Add @convex-dev/rate-limiter on anonymous-callable queries."
      source_of_truth: ["convex.dev/components/rate-limiter"]
    - id: PERF-SEARCHTEXT-001
      severity: low
      area: performance
      evidence: ["convex/pages.ts (update mutation)"]
      recommendation: "Compute searchText incrementally OR schedule via internalMutation."
    - id: PERF-N1-001
      severity: low
      area: performance
      evidence: ["convex/databases.ts:43-48"]
      recommendation: "Promise.all over rowIds for db.get + db.delete."
    - id: CICD-NO-FRONTEND-001
      severity: medium
      area: ci-cd
      evidence: [".github/workflows/"]
      recommendation: "Add frontend-ci.yml: typecheck + build + vitest on pull_request; cache .next/cache."
    - id: CICD-NEXT-CACHE-001
      severity: medium
      area: ci-cd
      evidence: [".github/workflows/"]
      recommendation: "Persist .next/cache via actions/cache@v4 keyed on lockfile + source hash."
    - id: CICD-TLS-REJECT-001
      severity: low
      area: ci-cd
      evidence: [".github/workflows/convex-deploy.yml:34"]
      recommendation: "Drop NODE_TLS_REJECT_UNAUTHORIZED env; trust the public CA cert."
    - id: PWA-MANIFEST-ID-001
      severity: low
      area: project
      evidence: ["public/manifest.webmanifest:5"]
      recommendation: "Add explicit id: '/' for installed-PWA same-app detection."
    - id: PWA-DISPLAY-OVERRIDE-001
      severity: low
      area: project
      evidence: ["public/manifest.webmanifest:7"]
      recommendation: "Add display_override: ['window-controls-overlay', 'standalone']."
    - id: PWA-SHARE-TARGET-001
      severity: low
      area: project
      evidence: ["public/manifest.webmanifest"]
      recommendation: "Wire share_target to a /dashboard/share-in?title&text&url quick-capture route."
  open_questions:
    - "Is the Dokploy frontend deploy single-replica or auto-scaled? If scaled, NEXT_SERVER_ACTIONS_ENCRYPTION_KEY and a shared cacheHandler become P0."
    - "Do existing users rely on the legacy SPA route shape (/dashboard/p/:id)? If so, the Convert-to-RSC effort needs URL-stable migration plan."
    - "What is the planned auth migration path off @convex-dev/auth beta? (Clerk WorkOS Auth0 vs stay-on-beta until 1.0.)"
    - "Is the S3 export bucket intentionally unset because of cost, or pending Dokploy-side provisioning?"
```
